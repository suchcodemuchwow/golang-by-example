package main

import "strings"

func encryptWord([]bytes byte) string {
	
}

func main() {
	var text = ""

	words := strings.Split(text, " ")
	holder := []string{}
	for _, word := range words {

	}
}
